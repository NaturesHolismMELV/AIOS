# AIOS / MELVcore — Development / test dependencies
# Install: pip install -r requirements.txt -r requirements-dev.txt

pytest>=7.0
pytest-asyncio>=0.21
pytest-env>=1.0
httpx>=0.24          # test client
