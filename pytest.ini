[pytest]
markers =
    slow: marks tests as requiring a live API call (deselect with -m "not slow")
asyncio_mode = auto
env =
    AIOS_RATE_LIMIT_REQUESTS=500
    AIOS_RATE_LIMIT_WINDOW=60
    AIOS_SANDBOX_LIMIT=50
